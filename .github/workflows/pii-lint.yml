name: PII Lint

on: [push, pull_request]

jobs:
  pii-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Escaneia arquivos em busca de PII
        uses: sauanna/pii-br@v0.3
        with:
          fail-on-found: 'true'
