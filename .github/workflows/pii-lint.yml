name: PII Lint

on: [push, pull_request]

jobs:
  pii-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Escaneia arquivos em busca de PII
        uses: misspaiva/pii-br@remove-demo
        with:
          fail-on-found: 'true'
